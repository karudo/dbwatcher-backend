version: '3.1'

services:
  postgres:
    image: postgres:10.5
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  mongo:
    image: mongo
    ports:
    - "27017:27017"
